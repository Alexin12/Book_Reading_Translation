# Per-Book Term List and contextual chunking for consistency

Translation is done in chunks that carry surrounding context, and each Book has a Term List (auto-extracted, user-correctable) that pins the agreed Translation of recurring names and terms. This keeps terminology consistent across the whole Book — the main quality advantage of using an LLM over a plain machine-translation API — at the cost of some added pipeline complexity.
